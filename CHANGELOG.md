# Changelog

All notable changes to `@absolutejs/ai`.

This file is generated by `absolute-changelog` from the entries in
`changelog/`. Edit an entry, not this file — and add new ones under
`changelog/unreleased/`.

## 0.2.1 — 2026-09-16

### Fixed

- **Preserve provider output reservations and context-error recognition when routing models** (`createAIProviderRouter`)

## 0.2.0 — 2026-09-16

### Added

- **Expose each tool-loop turn response metadata so consumers can price the actual served processing tier** (`streamAIWithTools`, `StreamAIWithToolsEvent`)
- **Route exact model IDs to the same provider for token counting, capacity checks, preparation and generation** (`createAIProviderRouter`)

## 0.1.3 — 2026-09-16

### Fixed

- **Exclude OpenAI cache-write tokens from ordinary input so cache-aware billing never charges the same tokens twice** (`openai`, `openaiResponses`)

## 0.1.2 — 2026-09-16

### Fixed

- **Preserve every parallel OpenAI tool result and subsequent conversation turn without overwriting expanded messages** (`openai`)

## 0.1.1 — 2026-09-16

### Added

- **Allow a separate preparation model with its own verified capacity while preserving the final answer model and invalidating notes when the preparation model changes** (`PrepareAITextInputOptions`, `prepareAITextInput`, `prepareAITextInputStep`)

### Fixed

- **Fill preparation sections with bounded tokenizer-checked refinement, avoid duplicate final counting, and honor cancellation before generation** (`prepareAITextInput`, `prepareAITextInputStep`)

## 0.1.0 — 2026-09-16

### Added

- **Add one-section text preparation steps for durable workers, with explicit pending/ready outcomes and checkpoint persistence before yielding** (`prepareAITextInputStep`, `AITextPreparationStep`)
- **Budget final retrieval instructions and tools before compacting text, with matching checkpoint invalidation** (`PrepareAITextInputOptions`, `prepareAITextInput`)

### Changed

- **Validate model capacity by default across generation, structured output, WebSocket and SSE entry points; reserve token headroom and support one protocol-preserving context recovery attempt. Providers without counting capability now fail explicitly; contextPolicy: false opts into raw behavior.** (`generateAI`, `generateAIWithTools`, `generateObjectAI`, `streamAI`, `streamAIToSSE`, `streamAIWithTools`, `inspectAIContext`, `createAIStoredToolResultRecovery`, `StreamAIWithToolsOptions`, `GenerateAIOptions`, `GenerateAIResult`, `GenerateObjectAIOptions`, `AIProviderConfig`, `StreamAIOptions`, `AIChatPluginConfig`)

### Fixed

- **Preserve conversation origin across long-text preparation sections, distinguish source facts from embedded instructions, reserve more of the available context for factual notes, and invalidate checkpoints from the previous preparation policy** (`prepareAITextInput`, `prepareAITextInputStep`)

## 0.0.58 — 2026-09-15

### Added

- **Keep compacted text useful with read-only original-source search, checkpointed ingestion, and capacity-checked terminal tool loops** (`createAITextSource`, `prepareAITextInput`, `PrepareAITextInputOptions`, `streamAIWithTools`, `StreamAIWithToolsOptions`)

## 0.0.57 — 2026-09-15

### Added

- **Add model-specific token capacity, optional provider capacity configuration, and recoverable long-text preparation** (`inspectAIInput`, `prepareAITextInput`, `AIProviderConfig`, `OpenAIConfig`, `OpenAIResponsesConfig`)

### Internal

- **Regenerate the OpenRouter client declaration with equivalent reordered response union types** (`createOpenRouterClient`)
